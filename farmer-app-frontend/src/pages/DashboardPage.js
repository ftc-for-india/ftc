import React from "react";
import { Typography, Container } from "@mui/material";

const DashboardPage = () => {
  return (
    <Container>
      <Typography variant="h4">Welcome to Farmer's Market</Typography>
      <Typography variant="body1">Find fresh products directly from farmers!</Typography>
    </Container>
  );
};

export default DashboardPage;
